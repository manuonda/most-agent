---
mantis: 202284
proyecto: PROD - GEINS - YPF EESS
estado: en_progreso
fecha_actualizacion: 2026-09-10
---

## Qué se hizo

Diagnóstico y Fase 1 del error 404 al ingresar al tipo de usuario GESTOR desde
`https://retail.instalaciones.ypf.ponchocompliance.com/app/gestores`
(pantalla de administración de tipo de usuarios, ambiente retail YPF).

### Causa raíz (diagnóstico final, corregido durante la sesión)

El repo de trabajo (`geins-ypf/web`, este proyecto) tiene mergeada la capa de
negocio/persistencia de `Gestor` (Mantis #191830) pero como un **stub
mínimo** (solo `getEntidadGestorMost()` / `getByNombreRazonSocial()`). La
capa web (`GestorController`, JSPs) **no existe en ningún lado de este
repo** — ni en `test`, ni `master`, ni `release`.

El diagnóstico inicial (una rama vieja `feature/mantis_0194642` sin mergear
*en este mismo repo*) resultó incompleto: esa rama es efectivamente vieja y
tiene solo 2 commits del feature. La fuente real y completa está en **otro
repositorio separado** (sin historia git compartida):
`producto/geins/web` (local: `/home/manuonda/projects/most/geins_producto/web`),
donde el feature Gestor está mergeado en `origin/master` de forma completa y
madura — 35 commits bajo Mantis #194642 (I a X + varios bugfixes
posteriores). Confirmado con `git ls-tree`/`git log` que no hay overlap de
objetos git entre ambos repos.

### Fase 1 — implementada en esta sesión (ver Notas de testing)

Se portó desde `producto/master` (recortando todo lo relacionado a la
sub-feature de vinculación Gestor↔EmpresaGestora, ver Fase 2):

- Nuevos: `GestorController.java`, `GestorValidator.java`, `GestorDTO.java`,
  `gestores.jsp`, `gestorAlta.jsp` + partials
  (`gestor/{datosGestor,domicilio,datosContacto,otrosContactosGestor}.jsp`,
  `modalOtrosContactosGestor.jsp`), `GEINS_DBScripts/mantis_202284.sql`
  (inserts en tabla `permiso` para el menú — **no ejecutado**, pendiente que
  el developer lo corra contra la base de YPF).
- Modificados (reemplazo wholesale del stub por la versión completa,
  recortando lo fuera de alcance): `Gestor.java` (entidad),
  `IGestorBusiness`/`GestorBusinessImpl`, `IGestorDAO`/`GestorDAOImpl`,
  `messages.properties`.
- Nota: `gestorList.jsp`/`gestorNewEdit.jsp` (mencionados en el diagnóstico
  inicial) son archivos huérfanos en el repo origen, no se portaron — las
  vistas reales son `gestores.jsp`/`gestorAlta.jsp`.
- Code review (`geins-code-review`) encontró y se corrigieron: mensaje de
  error que decía "Distribuidor" en vez de "Gestor" al validar DNI duplicado
  (faltaba la clave `gestor.documento.duplicado`), un campo autowired sin
  usar (`iConfTipoDocumentoBusiness`), y un `@ModelAttribute` con nombre
  inconsistente (`"distribuidor"` → `"entidad"`).
- Build: `mvn clean compile` desde `GEINS_Jars` (Java 11) — OK, antes y
  después de los fixes de la revisión.

## Notas de testing / pendientes

- **Todo el trabajo de la Fase 1 está sin commitear** en el worktree
  `/home/manuonda/projects/most/geins_producto_ypf/worktrees/mantis_0202284`
  (rama `feature/mantis_0202284`, base `origin/test` actualizado). No se
  hizo commit ni push — pendiente confirmación del developer.
- No se probó la UI en navegador (sin entorno para levantar Tomcat en esta
  sesión) — recomendado probar en test antes de mergear.
- Pendiente ejecutar `GEINS_DBScripts/mantis_202284.sql` contra la base de
  YPF — sin esto el link "Gestores" no aparece en el menú aunque el código
  esté andando.
- **Fase 2 (pendiente, fuera de este Mantis)** — vinculación
  Gestor↔EmpresaGestora (`EmpresaGestoraGestorController`, entidad/tabla
  nueva `EntidadGestor`, cambios en `EmpresaGestoraBusinessImpl`,
  `TipoUsuario.esSistemaOGestor()`, `EmpresaGestoraDTO`,
  `EntidadDomicilioDTO`/`EntidadDomicilio`, migración SQL de la tabla nueva).
- **Fase 3 (pendiente, fuera de este Mantis)** — `GestorStrategy.java` en
  YPF ya tiene una implementación propia y distinta a la del repo origen
  (usa `buscarEmpresasGestorasDeUnDistribuidor` en vez del enfoque basado en
  `EntidadGestor` de `producto`). No es bloqueante — compila igual porque no
  está en el camino de lo portado en Fase 1 — pero si se hace la Fase 2 hay
  que decidir si se unifica con el enfoque de `producto` o se mantiene el
  propio de YPF.
- No hay Fase 4 identificada por ahora; si al implementar la Fase 2 aparece
  algo nuevo, se suma ahí.
- No se descargó el adjunto `image.png` del issue (id 125693): la descarga
  vía `mantis-api.sh download` devolvió 404 con varias rutas probadas
  (`125693`, `files/125693`, `issues/202284/files/125693`).

## Resumen enviado a Mantis

(pendiente — no se posteó nota a Mantis en esta sesión)
