---
mantis: 202284
proyecto: PROD - GEINS - YPF EESS
estado: en_progreso
fecha_actualizacion: 2026-09-11
---

## Qué se hizo

Diagnóstico y Fases 1 y 2 del error 404 al ingresar al tipo de usuario GESTOR desde
`https://retail.instalaciones.ypf.ponchocompliance.com/app/gestores`
(pantalla de administración de tipo de usuarios, ambiente retail YPF).

### Causa raíz (diagnóstico final)

El repo de trabajo (`geins-ypf/web`, este proyecto) tenía mergeada la capa de
negocio/persistencia de `Gestor` (Mantis #191830) pero como un **stub
mínimo** (solo `getEntidadGestorMost()` / `getByNombreRazonSocial()`). La
capa web (`GestorController`, JSPs) no existía en ningún lado de este repo
(ni `test`, ni `master`, ni `release`).

La fuente completa y madura está en **otro repositorio separado** (sin
historia git compartida): `producto/geins/web`
(local: `/home/manuonda/projects/most/geins_producto/web`), donde el feature
Gestor está mergeado en `origin/master` — 35 commits bajo Mantis #194642
(I a X + bugfixes).

### Fase 1 — commit `a9a79654b` ("Fase I: Agregar controlador web, validador y vistas de Gestor")

Portado desde `producto/master`, recortando lo relacionado a la vinculación
Gestor↔EmpresaGestora (Fase 2):

- Nuevos: `GestorController.java`, `GestorValidator.java`, `GestorDTO.java`,
  `gestores.jsp`, `gestorAlta.jsp` + partials, `GEINS_DBScripts/mantis_202284.sql`
  (inserts en tabla `permiso` para el menú).
- Modificados: `Gestor.java`, `IGestorBusiness`/`GestorBusinessImpl`,
  `IGestorDAO`/`GestorDAOImpl`, `messages.properties`.
- Code review corrigió: mensaje "Distribuidor" en vez de "Gestor" en
  duplicado de DNI, campo autowired sin usar, `@ModelAttribute` inconsistente.

### Fase 2 — commit `1c4ae70c4` ("Fase II: Agregar vinculacion Gestor-EmpresaGestora")

Portado desde `producto/master`:

- Nuevos: `EmpresaGestoraGestorController.java`, entidad/tabla `EntidadGestor`
  + `EntidadGestorDTO`, `IEntidadGestorBusiness`/`Impl`,
  `IEntidadGestorDAO`/`Impl`, 3 JSPs de vinculación
  (`empresasGestorasDelGestor*.jsp`).
- Modificados: `EmpresaGestoraBusinessImpl`/`IEmpresaGestoraBusiness` (nuevo
  método `buscarEmpresasGestorasDelGestor`), `TipoUsuario.esSistemaOGestor()`,
  `EmpresaGestoraDTO`/`EmpresaGestoraDTOJson`/`EntidadDomicilioDTO` (campos
  nuevos), `EntidadDomicilio` (FK real `id_entidad_gestor`, no transitorio).
- Se restauró en los archivos de la Fase 1 (`Gestor`, `GestorBusinessImpl`,
  `IGestorBusiness`, `GestorDAOImpl`, `IGestorDAO`) todo lo que se había
  recortado por depender de `EntidadGestor` (que en Fase 1 todavía no existía).
- `GEINS_DBScripts/mantis_202284.sql` extendido con `CREATE TABLE
  entidad_gestor`, `ALTER TABLE entidad_domicilio ADD ID_ENTIDAD_GESTOR`, y
  permiso `CREATE_CONTACTO_GESTOR`.
- `GestorStrategy.java` **no se tocó** — decisión explícita del developer,
  queda para la Fase 3 (ver abajo).
- Code review corrigió indentación mezclada (tabs+espacios) en 3 métodos
  restaurados. Detectó pero NO corrigió un bug pre-existente del repo origen:
  `EntidadGestorDAOImpl.getGestoresProjection(...)` referencia un atributo
  `tipoGestor` que no existe en `Gestor`/`Entidad` — compila (Criteria API no
  tipa en compile-time) pero rompería en runtime si se ejecutara. Confirmado
  que nada en el código portado lo invoca (código muerto por ahora).
- Build: `mvn clean compile` desde `GEINS_Jars` (Java 11) — OK.

### Auditoría de completitud (post Fase 1 + 2) — commit `f55ed935b`

Se comparó archivo por archivo `producto/master` vs. el worktree para todo lo
relacionado a "gestor" (excluyendo `EmpresaGestora*`), y se auditaron TODAS
las claves `spring:message` de las 10 JSPs portadas contra
`messages.properties`. Resultado:

- **Gap real encontrado y corregido**: faltaban 8 claves de mensaje
  (`gestor`, `gestor.datos`, `menu.gestor`, y 5 de la pantalla de vinculación
  Gestor-EmpresaGestora: `gestor.empresas.gestoras.{nota,contactos,
  contactos.gestor,contacto.gestor,contactos.para.empresa.gestora}`) — sin
  esto se verían placeholders rotos en pantalla.
- **Script SQL**: confirmado que `mantis_202284.sql` tiene el 100% de lo que
  trae `mantis_194642.sql` del origen (tabla, alter, 3 permisos). Nada
  faltante.
- **Falsos positivos descartados**: `ConfTipoGestor`, `GestorDocumento`,
  `ResultadoGestorDTO`, `IConfTipoGestorBusiness`, `ConfActuacionTipoGestor`
  son código muerto en el propio repo origen (solo los usan
  `gestorList.jsp`/`gestorNewEdit.jsp`, los mismos archivos huérfanos ya
  descartados en Fase 1) — no hace falta portarlos.
- **Completado** (commit `a67dd060d`): `messages_en.properties` no tenía
  ninguna clave `gestor.*` — gap pre-existente en el proyecto (ni el propio
  repo origen `producto/geins/web` las tiene en su `messages_en.properties`,
  no es algo que rompimos). Se agregaron las 14 claves equivalentes,
  traducidas siguiendo el estilo ya usado para `distribuidor.*`/`proveedor.*`
  (Gestor → Manager). No existe `messages_pt.properties` en este repo
  (geins-ypf), por lo que portugués no aplica acá.

## Notas de testing / pendientes

- Fase 1, Fase 2 y el fix de mensajes están commiteados en el worktree
  `/home/manuonda/projects/most/geins_producto_ypf/worktrees/mantis_0202284`
  (rama `feature/mantis_0202284`, sobre `origin/test`). **No se hizo push.**
- No se probó la UI en navegador en ninguna fase (sin entorno para levantar
  Tomcat en esta sesión) — recomendado probar en test antes de mergear:
  alta de Gestor, listado, vinculación con EmpresaGestora, logo, domicilios.
- Pendiente ejecutar `GEINS_DBScripts/mantis_202284.sql` contra la base de
  YPF (permisos de menú + tabla `entidad_gestor` + alter de
  `entidad_domicilio`) — sin esto el código no tiene efecto visible en
  producción.
- **Fase 3 (en análisis con QA, todavía sin decidir ni aplicar)** —
  `GestorStrategy.findEmpresasGestoras(...)` en YPF usa
  `buscarEmpresasGestorasDeUnDistribuidor(dto, null, null, null, tipoUsuario)`
  con todos los filtros en `null` → **no filtra nada, devuelve TODAS las
  Empresas Gestoras activas del sistema** a cualquier usuario tipo GESTOR
  (sobre-exposición de datos, no solo un detalle de estilo). `producto` usa
  en cambio `iEntidadGestorBusiness.getEmpresasGestoras(idEntidad)`, que sí
  filtra por la vinculación real en `entidad_gestor`.

  Relevamiento de datos hecho con el developer (vía consultas SQL directas,
  MySQL local en `localhost:3306` con XAMPP, user `root` sin password, y
  contra bases productivas):
  - Test local `geins_producto_ypf_test_2`: 8 usuarios GESTOR, `entidad_gestor`
    con 4 filas que cubren los 2 Gestor existentes (id_entidad 33 y 43) →
    cambio seguro ahí.
  - 3 bases productivas en total. Una es `geins_refineria_ypf` (query de
    usuarios GESTOR corrida ahí, resultado pendiente de compartir — no
    documentar números todavía). Antes se relevó (sin identificar a cuál de
    las 3 corresponde) una productiva con 2 usuarios GESTOR, ambos con
    `id_entidad = 28`. Por separado se confirmó que 2 de las 3 productivas
    tienen `entidad_gestor` vacía, y la 3ra tiene al menos 1 fila real:
    `id_gestor=36 → id_empresa_gestora=30`, cargada a mano el 2025-11-13 por
    `ygonzalez` (el mismo que reportó este Mantis — probablemente ya había
    detectado el problema y parchó un caso puntual).
  - **Riesgo si se aplica el cambio sin backfill**: en 2 de las 3 productivas,
    todos los usuarios GESTOR pasarían de "ven todo" a "ven nada" hasta que
    se cargue su vínculo.
  - Plan acordado con el developer: (1) cargar manualmente los `INSERT INTO
    entidad_gestor` para cada Gestor activo en las 3 productivas — sin
    riesgo, no depende del cambio de código — una vez que QA/negocio
    confirme a qué Empresa(s) Gestora(s) corresponde cada uno; (2) recién
    después, aplicar el cambio de una línea en `GestorStrategy.java`.
  - Documento armado para QA con esta comparación, la tabla de relevamiento
    y un checklist de lo que falta confirmar: artifact "Alcance de Gestores"
    — https://claude.ai/code/artifact/f0330e3a-2523-4fa2-9f30-7c461485da74
    (privado, pendiente de completar con los datos de `geins_refineria_ypf`
    y compartir con el equipo).
- No hay Fase 4 identificada.
- No se descargó el adjunto `image.png` del issue (id 125693): la descarga
  vía `mantis-api.sh download` devolvió 404 con varias rutas probadas.

## Resumen enviado a Mantis

Nota `616571` posteada el 2026-09-11 (5:00 hs registradas), con adjunto
`mantis_202284.sql`. Estado del issue sin cambios (sigue "se necesitan más
datos" en Mantis — no corresponde marcarlo resuelto, sigue en desarrollo):

> Resumen del trabajo:
>
> - Se identificó la causa del error 404 al administrar usuarios de tipo GESTOR: la funcionalidad completa de gestión de Gestores nunca había llegado a este ambiente, aunque sí estaba disponible en el producto base.
> - Se incorporó la pantalla de administración de Gestores completa: alta, edición, listado, carga de logo y domicilios/contactos.
> - Se incorporó la funcionalidad para vincular un Gestor con la o las Empresas Gestoras con las que trabaja, incluyendo la gestión de sus contactos.
> - Se completaron las traducciones de los textos nuevos en español e inglés.
> - Se está analizando junto con QA un ajuste adicional sobre qué información ve un usuario Gestor al loguearse, para asegurar que solo vea las Empresas Gestoras que le corresponden (actualmente ve la de todas). Este punto está en revisión y todavía no se aplicó ningún cambio sobre esto.
>
> Scripts adjuntos:
> - mantis_202284.sql — habilita el permiso de menú para que aparezca la opción "Gestores", y crea la tabla de vinculación entre Gestor y Empresa Gestora.
>
> Pendiente:
> - Definir con QA/negocio el ajuste de alcance de datos mencionado arriba antes de aplicarlo.
>
> Estado: en desarrollo todavía.
>
> Horas trabajadas: 5:00
